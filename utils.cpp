#include <iostream>
#include "include/utils.h"

using namespace std;

int main() {
    int *a = (int*)malloc(2*sizeof(int));
    cout<<nexti<<endl;
    char *b = (char*)malloc(2*sizeof(char));
    cout << nexti;
}
